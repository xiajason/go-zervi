# Zervigo 数据库适配器完整文档

## 📋 文档概述

**创建时间**: 2025-01-29  
**目的**: 记录所有数据库适配器的实现细节、差异分析和未来迭代升级计划  
**维护**: 持续更新

---

## 🗄️ 支持的数据库类型

### 当前已实现的数据库适配器

| 数据库类型 | 适配器文件 | 状态 | 用途 | 驱动库 |
|-----------|-----------|------|------|--------|
| **MySQL** | `mysql.go` | ✅ 已实现 | 关系型数据库（备用） | `gorm.io/driver/mysql` |
| **PostgreSQL** | `postgresql.go` | ✅ 已实现 | 关系型数据库（主力） | `gorm.io/driver/postgres` |
| **Redis** | `redis.go` | ✅ 已实现 | 内存数据库/缓存 | `github.com/go-redis/redis/v8` |
| **Neo4j** | `neo4j.go` | ✅ 已实现 | 图数据库 | `github.com/neo4j/neo4j-go-driver/v5` |
| **SQLite3** | ❌ 未在core中实现 | ⚠️ 通过其他方式使用 | 轻量级数据库 | 在业务服务中直接使用 |

### 数据库管理器统一接口

**文件**: `shared/core/database/manager.go`

```go
type Manager struct {
    MySQL      *MySQLManager
    Redis      *RedisManager
    PostgreSQL *PostgreSQLManager
    Neo4j      *Neo4jManager
    config     Config
}
```

---

## 🔍 MySQL vs PostgreSQL 适配器详细对比

### 最大区别：数据库迁移策略

#### 🔴 MySQL: 安全迁移模式（Safe Migration）

```go
// mysql.go:75-99
func (mm *MySQLManager) Migrate(models ...interface{}) error {
    // 检查表是否存在，如果存在则跳过迁移
    for _, model := range models {
        stmt := &gorm.Statement{DB: mm.db}
        if err := stmt.Parse(model); err != nil {
            return fmt.Errorf("解析模型失败: %w", err)
        }

        // 检查表是否存在
        if mm.db.Migrator().HasTable(stmt.Schema.Table) {
            // 表已存在，只添加缺失的列，不修改现有约束
            if err := mm.db.Migrator().AutoMigrate(model); err != nil {
                // 如果迁移失败，记录错误但不中断
                fmt.Printf("警告: 表 %s 迁移失败: %v\n", stmt.Schema.Table, err)
            }
        } else {
            // 表不存在，正常创建
            if err := mm.db.Migrator().CreateTable(model); err != nil {
                return fmt.Errorf("创建表失败: %w", err)
            }
        }
    }
    return nil
}
```

**特点**:
- ✅ **安全检查**: 先检查表是否存在
- ✅ **容错处理**: 迁移失败只记录警告，不中断程序
- ✅ **增量更新**: 只添加缺失的列，不修改现有约束
- ✅ **外键约束**: 禁用外键约束以避免迁移冲突

#### 🟡 PostgreSQL: 标准迁移模式（Standard Migration）

```go
// postgresql.go:105-108
func (pm *PostgreSQLManager) Migrate(models ...interface{}) error {
    return pm.db.AutoMigrate(models...)
}
```

**特点**:
- ⚠️ **直接迁移**: 直接调用GORM的AutoMigrate
- ⚠️ **无安全检查**: 不检查表是否存在
- ⚠️ **可能破坏数据**: 如果表结构冲突可能导致错误

**⚠️ 建议**: PostgreSQL适配器应该采用MySQL的安全迁移模式

---

### 其他重要区别

#### 1. GORM配置差异

**MySQL**:
```go
db, err := gorm.Open(mysql.Open(dsn), &gorm.Config{
    Logger:                                   logger.Default.LogMode(config.LogLevel),
    DisableForeignKeyConstraintWhenMigrating: true,  // ⚠️ 禁用外键约束
})
```

**PostgreSQL**:
```go
db, err := gorm.Open(postgres.Open(dsn), &gorm.Config{
    Logger: logger.Default.LogMode(config.LogLevel),
})
```

#### 2. 连接字符串格式

**MySQL**: TCP格式
```go
dsn := fmt.Sprintf("%s:%s@tcp(%s:%d)/%s?charset=%s&parseTime=True&loc=Local",
    config.Username, config.Password, config.Host, config.Port, 
    config.Database, config.Charset)
```

**PostgreSQL**: DSN格式（特殊处理空密码）
```go
dsn := fmt.Sprintf("host=%s port=%d user=%s dbname=%s sslmode=%s",
    config.Host, config.Port, config.Username, config.Database, config.SSLMode)
if config.Password != "" {
    dsn = fmt.Sprintf("host=%s port=%d user=%s password=%s dbname=%s sslmode=%s",
        config.Host, config.Port, config.Username, config.Password, 
        config.Database, config.SSLMode)
}
```

#### 3. 配置结构差异

**MySQL特有字段**:
- `Charset`: 字符集（如utf8mb4）

**PostgreSQL特有字段**:
- `SSLMode`: SSL模式（如disable, require, verify-full）
- 默认数据库名处理（空时设为`zervigo_mvp`）

#### 4. API方法差异

**PostgreSQL独有方法**:
- `GetSQLDB()`: 获取原生SQL连接
- `CreateVectorExtension()`: 创建向量扩展
- `CreateVectorIndex()`: 创建向量索引
- `VectorSearch()`: 向量搜索（AI功能）

---

## 📊 各数据库适配器特性对比

| 特性 | MySQL | PostgreSQL | Redis | Neo4j |
|------|-------|------------|-------|-------|
| **数据库类型** | 关系型 | 关系型 | 内存/缓存 | 图数据库 |
| **迁移策略** | ✅ 安全模式 | ⚠️ 标准模式 | ❌ 不支持 | ❌ 不支持 |
| **外键约束** | ✅ 禁用 | ⚠️ 启用 | ❌ 不支持 | ❌ 不支持 |
| **连接格式** | TCP | DSN | Redis协议 | Bolt协议 |
| **字符集** | ✅ 需要 | ❌ 不需要 | ❌ 不需要 | ❌ 不需要 |
| **SSL模式** | ❌ 不需要 | ✅ 需要 | ✅ 可选 | ✅ 可选 |
| **事务支持** | ✅ 支持 | ✅ 支持 | ❌ 不支持 | ✅ 支持 |
| **向量搜索** | ❌ 不支持 | ✅ 支持 | ❌ 不支持 | ❌ 不支持 |
| **图查询** | ❌ 不支持 | ❌ 不支持 | ❌ 不支持 | ✅ 支持 |
| **缓存能力** | ❌ 不支持 | ❌ 不支持 | ✅ 支持 | ❌ 不支持 |
| **健康检查** | ✅ 支持 | ✅ 支持 | ✅ 支持 | ✅ 支持 |

---

## 🔧 Redis 适配器特点

**文件**: `shared/core/database/redis.go`

**主要功能**:
- ✅ 键值存储（Set/Get）
- ✅ 哈希表（HSet/HGet/HGetAll）
- ✅ 列表（LPush/RPush/LPop/RPop）
- ✅ 集合（SAdd/SMembers）
- ✅ 有序集合（ZAdd/ZRange）
- ✅ 管道操作（Pipeline）
- ✅ 连接池管理

**使用场景**:
- 用户会话存储
- API响应缓存
- 临时数据存储
- 分布式锁

---

## 🕸️ Neo4j 适配器特点

**文件**: `shared/core/database/neo4j.go`

**主要功能**:
- ✅ Cypher查询执行
- ✅ 节点创建和查询
- ✅ 关系创建和查询
- ✅ 图搜索（GraphSearch）
- ✅ 节点和关系更新/删除

**使用场景**:
- 社交网络关系
- 推荐系统
- 知识图谱
- 复杂关系查询

---

## 📝 未来迭代升级计划

### 第一阶段：统一迁移策略（高优先级）

**目标**: 统一MySQL和PostgreSQL的迁移策略

**任务**:
1. ✅ 记录MySQL的安全迁移模式
2. ⚠️ **待实现**: PostgreSQL采用安全迁移模式
3. ⚠️ **待实现**: 统一迁移错误处理

**实现建议**:
```go
// 建议的PostgreSQL安全迁移实现
func (pm *PostgreSQLManager) Migrate(models ...interface{}) error {
    for _, model := range models {
        stmt := &gorm.Statement{DB: pm.db}
        if err := stmt.Parse(model); err != nil {
            return fmt.Errorf("解析模型失败: %w", err)
        }
        
        if pm.db.Migrator().HasTable(stmt.Schema.Table) {
            // 表存在，安全迁移
            if err := pm.db.Migrator().AutoMigrate(model); err != nil {
                fmt.Printf("警告: 表 %s 迁移失败: %v\n", stmt.Schema.Table, err)
                // 可选：记录到日志系统
            }
        } else {
            // 表不存在，创建
            if err := pm.db.Migrator().CreateTable(model); err != nil {
                return fmt.Errorf("创建表失败: %w", err)
            }
        }
    }
    return nil
}
```

### 第二阶段：增强功能（中优先级）

**目标**: 增强各数据库适配器的功能

**任务**:
1. **MySQL适配器**:
   - ⚠️ 添加DEBUG日志输出
   - ⚠️ 添加连接池监控
   - ⚠️ 添加慢查询日志

2. **PostgreSQL适配器**:
   - ✅ 已有向量搜索功能
   - ⚠️ 添加全文搜索支持
   - ⚠️ 添加JSONB查询优化
   - ⚠️ 添加RLS（Row Level Security）支持

3. **Redis适配器**:
   - ⚠️ 添加集群支持
   - ⚠️ 添加Sentinel支持
   - ⚠️ 添加Stream支持

4. **Neo4j适配器**:
   - ⚠️ 添加事务支持优化
   - ⚠️ 添加批量操作支持
   - ⚠️ 添加APOC插件支持

### 第三阶段：SQLite3适配器（低优先级）

**目标**: 在core中实现SQLite3适配器

**当前状态**: SQLite3在业务服务中直接使用，未在core中实现统一适配器

**建议实现**:
```go
// 建议的SQLite3适配器结构
type SQLite3Manager struct {
    db     *gorm.DB
    config SQLite3Config
}

type SQLite3Config struct {
    FilePath string
    // ... 其他配置
}
```

**使用场景**:
- 用户个人数据隔离
- 本地开发和测试
- 敏感数据存储
- 离线数据支持

### 第四阶段：其他数据库支持（可选）

**潜在支持的数据库**:

1. **MongoDB** (文档数据库)
   - 用途: 非结构化数据存储
   - 驱动: `go.mongodb.org/mongo-driver`

2. **ClickHouse** (列式数据库)
   - 用途: 大数据分析
   - 驱动: `github.com/ClickHouse/clickhouse-go`

3. **Cassandra** (宽列数据库)
   - 用途: 分布式数据存储
   - 驱动: `github.com/gocql/gocql`

4. **SQL Server** (关系型数据库)
   - 用途: 企业级数据库
   - 驱动: `github.com/denisenkom/go-mssqldb`

5. **Oracle** (关系型数据库)
   - 用途: 企业级数据库
   - 驱动: `github.com/godror/godror`

---

## 🎯 最佳实践建议

### 1. 统一迁移策略

**建议**: 所有关系型数据库都应该采用安全迁移模式
- ✅ 避免破坏现有数据
- ✅ 支持增量更新
- ✅ 容错处理

### 2. 统一错误处理

**建议**: 所有适配器都应该有统一的错误处理机制
- ✅ 迁移错误记录但不中断
- ✅ 连接错误重试机制
- ✅ 详细的错误日志

### 3. 统一健康检查

**建议**: 所有适配器都应该实现健康检查
- ✅ 统一的健康检查接口
- ✅ 详细的健康状态信息
- ✅ 自动恢复机制

### 4. 统一配置管理

**建议**: 所有适配器都应该有统一的配置结构
- ✅ 环境变量支持
- ✅ 配置文件支持
- ✅ 默认值处理

### 5. 统一日志输出

**建议**: 所有适配器都应该有DEBUG日志
- ✅ 连接信息日志
- ✅ 迁移操作日志
- ✅ 错误详细日志

---

## 📚 参考文档

### 相关文档
- `docs/MYSQL_POSTGRESQL_ADAPTER_DIFFERENCES.md` - MySQL和PostgreSQL详细对比
- `docs/DATABASE_ARCHITECTURE_OPTIMIZATION.md` - 数据库架构优化方案
- `docs/POSTGRESQL_DATABASE_CONFIG_REPORT.md` - PostgreSQL配置报告

### 代码位置
- `shared/core/database/` - 所有数据库适配器实现
- `shared/core/database/manager.go` - 统一数据库管理器
- `shared/core/core.go` - 数据库初始化配置

---

## 🔄 版本历史

| 版本 | 日期 | 更新内容 |
|------|------|----------|
| v1.0 | 2025-01-29 | 初始文档创建，记录MySQL和PostgreSQL适配器差异 |
| v1.1 | 待定 | PostgreSQL适配器安全迁移模式实现 |
| v1.2 | 待定 | SQLite3适配器实现 |
| v1.3 | 待定 | 其他数据库支持（MongoDB、ClickHouse等） |

---

**文档维护者**: Zervigo开发团队  
**最后更新**: 2025-01-29  
**下次审查**: 完成PostgreSQL安全迁移模式后

