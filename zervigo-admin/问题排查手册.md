# 🔍 Zervigo Admin 问题排查手册

## ❌ 常见问题与解决方案

### 问题 1: "登录响应格式错误"

**错误提示**: 点击登录后提示"登录响应格式错误"

**原因**: 
1. 后端返回的数据格式与前端预期不符
2. 响应拦截器返回了错误的数据结构

**已修复**: ✅

**验证方法**:
```bash
# 测试后端登录接口
curl -X POST http://localhost:9000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

**预期响应**:
```json
{
  "code": 0,
  "message": "Login successful",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "user": {
      "id": 1,
      "username": "admin",
      "email": "admin@zervigo.com"
    }
  },
  "timestamp": 1730972400
}
```

**修复内容**:
- ✅ 更新了 `request.ts` 响应拦截器，兼容多种格式
- ✅ 修复了 `Login.vue` 中的响应数据访问路径
- ✅ 添加了详细的调试日志

---

### 问题 2: "无法连接服务器"

**错误提示**: Safari 浏览器显示"无法连接服务器localhost:5173"

**原因**: 
1. 访问端口错误 - 实际运行在 3000，不是 5173
2. Vite 开发服务器未启动

**解决方案**:
1. 查看终端输出，确认实际端口
2. 访问正确的地址：**http://localhost:3000**

**终端输出示例**:
```
VITE v7.2.0  ready in 395 ms
➜  Local:   http://localhost:3000/
```

---

### 问题 3: 控制台报错 "Cannot find module '@/xxx'"

**错误提示**: 
```
TypeError: Importing a module script failed
Cannot find module '@/services/MenuService'
```

**原因**: 
1. `@/` 路径别名未配置
2. TypeScript 路径映射缺失

**已修复**: ✅

**修复内容**:
- ✅ 在 `vite.config.ts` 中添加了路径别名
- ✅ 在 `tsconfig.app.json` 中添加了路径映射

**验证方法**:
```bash
npm run build
```

应该没有模块找不到的错误。

---

### 问题 4: 点击登录无反应

**可能原因**:
1. 后端服务未启动
2. 网络请求被CORS阻止
3. JavaScript 错误

**排查步骤**:

#### Step 1: 检查后端服务
```bash
curl http://localhost:9000/health
```

应该返回健康检查信息。

#### Step 2: 查看浏览器控制台
打开开发者工具（F12），查看：
- Console 标签：是否有错误
- Network 标签：login 请求状态

#### Step 3: 查看请求详情
在 Network 标签中找到 `login` 请求：
- Status: 应该是 200
- Response: 查看返回数据
- Headers: 查看 CORS 头

---

### 问题 5: 登录成功但页面空白

**可能原因**:
1. 路由跳转失败
2. Home 组件加载失败
3. 动态菜单加载失败

**排查步骤**:

#### Step 1: 查看控制台错误
```javascript
// 应该看到
登录响应: { token: "...", user: {...} }
登录成功！
```

#### Step 2: 查看 URL 是否变化
登录后 URL 应该从 `/login` 变为 `/home`

#### Step 3: 查看菜单加载日志
```javascript
✅ 菜单加载成功
[路由加载] 可用的模板文件: [...]
✅ 动态路由注册完成
```

---

## 🔧 完整排查流程

### 1. 环境检查

```bash
# Node.js 版本
node --version  # 应该 >= 18

# npm 版本
npm --version   # 应该 >= 9

# Go 版本
go version      # 应该 >= 1.20
```

### 2. 服务状态检查

```bash
# 后端健康检查
curl http://localhost:9000/health

# 菜单API测试
curl http://localhost:9000/api/v1/router/menu/list

# 登录API测试
curl -X POST http://localhost:9000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 3. 前端构建检查

```bash
cd /Users/szjason72/gozervi/zervigo.demo/zervigo-admin

# TypeScript 编译检查
npm run build

# 应该没有致命错误
```

### 4. 浏览器检查

打开开发者工具（F12），检查：

**Console 标签**:
- 有无 JavaScript 错误
- 有无模块加载失败
- 登录响应日志

**Network 标签**:
- login 请求状态
- 响应数据格式
- CORS 配置

**Application 标签**:
- LocalStorage 中是否有 token
- LocalStorage 中是否有 userInfo

---

## 📋 问题报告模板

如果遇到问题，提供以下信息：

### 环境信息
```
Node.js 版本: 
npm 版本:
Go 版本:
操作系统:
浏览器:
```

### 错误信息
```
浏览器控制台错误:
[粘贴控制台错误]

终端错误:
[粘贴终端错误]
```

### 后端状态
```bash
# 健康检查结果
curl http://localhost:9000/health

# 登录接口测试
curl -X POST http://localhost:9000/api/v1/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"admin","password":"admin123"}'
```

### 前端状态
```
访问地址: http://localhost:????
能否打开登录页: 是/否
点击登录反应:
控制台日志:
```

---

## 🎯 快速修复指令

### 重置并重新开始

```bash
cd /Users/szjason72/gozervi/zervigo.demo/zervigo-admin

# 清除依赖
rm -rf node_modules package-lock.json

# 重新安装
npm install

# 重新启动
npm run dev
```

### 检查代码问题

```bash
# TypeScript 类型检查
npm run build

# 查看具体错误
```

### 后端重启

```bash
cd /Users/szjason72/gozervi/zervigo.demo/shared/central-brain

# 停止当前运行的后端 (Ctrl+C)
# 重新启动
go run .
```

---

## 📚 相关文档

- `API响应格式说明.md` - API 数据格式详解
- `登录修复说明.md` - 登录功能修复详情
- `完整启动指南.md` - 前后端启动步骤
- `问题修复说明.md` - 之前问题的修复记录

---

**更新日期**: 2024-11-06  
**版本**: v2.1.2  
**状态**: ✅ 所有已知问题已修复  

