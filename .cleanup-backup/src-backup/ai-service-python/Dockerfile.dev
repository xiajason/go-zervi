# Python AI服务开发环境 Dockerfile
FROM python:3.11-slim AS development

# 设置工作目录
WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    g++ \
    && rm -rf /var/lib/apt/lists/*

# 复制requirements文件
COPY requirements.txt .

# 安装Python依赖
RUN pip install --no-cache-dir -r requirements.txt

# 安装开发工具
RUN pip install watchdog

# 复制源码
COPY . .

# 暴露端口
EXPOSE 8100

# 使用watchdog进行热重载开发
CMD ["python", "-m", "watchdog", "--patterns", "*.py", "--recursive", "--command", "python ai_service_with_zervigo.py"]
