# ⚠️ 配置说明 - 重要区分

## 🎯 核心概念区分

### 1. 数据库连接账号 vs 前端登录账号

这是**两个完全不同的概念**，请务必区分：

#### 📊 数据库连接账号（PostgreSQL 用户）
- **用途**: 应用程序连接数据库使用
- **用户名**: `vuecmf`
- **密码**: `vuecmf`
- **配置位置**: `/Users/szjason72/gozervi/zervigo.demo/configs/local.env`

```env
POSTGRESQL_USER=vuecmf
POSTGRESQL_PASSWORD=vuecmf
POSTGRESQL_DATABASE=zervigo_mvp
```

#### 👤 前端登录账号（应用用户）
- **用途**: 前端界面登录使用
- **用户名**: `admin`
- **密码**: `admin123`
- **存储位置**: 数据库表 `zervigo_auth_users`

---

## ✅ 正确的配置情况

### 数据库类型
- **前端访问**: PostgreSQL ✅
- **不是**: MySQL ❌

### 账号信息

| 类型 | 用户名 | 密码 | 用途 |
|------|--------|------|------|
| 数据库连接 | `vuecmf` | `vuecmf` | Go 服务连接 PostgreSQL |
| 前端登录 | `admin` | `admin123` | 网页登录系统 |

---

## 🔧 配置验证

### 1. 验证数据库连接
```bash
PGPASSWORD=vuecmf psql -h localhost -U vuecmf -d zervigo_mvp -c "SELECT version();"
```

✅ 如果成功，说明数据库连接账号配置正确

### 2. 验证前端登录账号
```bash
PGPASSWORD=vuecmf psql -h localhost -U vuecmf -d zervigo_mvp -c "SELECT username, email, status FROM zervigo_auth_users WHERE username = 'admin';"
```

应该看到：
```
 username |       email       | status 
----------+-------------------+--------
 admin    | admin@zervigo.com | active
```

### 3. 验证密码哈希
```bash
cd /Users/szjason72/gozervi/zervigo.demo/shared/core
go run ./cmd/check-password --hash '$2a$10$FuZxOzV9B12T7rpMmw5mX.dCXmR8K2S6zi9v1leHn1YlWyA2AHOZW' --passwords "admin123"
```

应该看到：
```
密码 'admin123' 匹配！
```

---

## 📝 我之前犯的错误

### ❌ 错误理解
我之前混淆了：
- **数据库用户** (`vuecmf`) 
- **应用用户** (`admin`)

### ❌ 错误假设
我错误地认为前端登录使用：
- 用户名: `vuecmf` 
- 密码: `vuecmf` 或 `123456`

### ✅ 实际情况
- **数据库连接**: `vuecmf` / `vuecmf`（用于 Go 服务连接数据库）
- **前端登录**: `admin` / `admin123`（用于网页登录）

---

## 🚀 正确的登录流程

### 启动服务后，访问前端

1. 打开浏览器访问: `http://localhost:8081`
2. 输入登录信息：
   - 用户名: `admin`
   - 密码: `admin123`
3. 点击登录

### 后台发生的事情

```
前端 (localhost:8081)
  ↓ POST /api/v1/auth/login
  ↓ {"username": "admin", "password": "admin123"}
  ↓
Central Brain (localhost:9000)
  ↓ 转发请求
  ↓
Auth Service (localhost:8207)
  ↓ 使用 vuecmf 连接 PostgreSQL
  ↓ SELECT * FROM zervigo_auth_users WHERE username = 'admin'
  ↓ 验证密码哈希
  ↓
PostgreSQL (localhost:5432/zervigo_mvp)
```

---

## 📊 完整的架构说明

### 数据流向

```
┌─────────────────────────────────────────────────┐
│           前端用户（你）                         │
│  用户名: admin                                   │
│  密码: admin123                                  │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│           前端应用 (localhost:8081)              │
│  Vue.js 应用                                     │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│      Central Brain (localhost:9000)             │
│  API 网关                                        │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│      Auth Service (localhost:8207)              │
│  使用 vuecmf/vuecmf 连接数据库                   │
└──────────────┬──────────────────────────────────┘
               │
               ▼
┌─────────────────────────────────────────────────┐
│  PostgreSQL (localhost:5432/zervigo_mvp)        │
│  数据库用户: vuecmf                              │
│  数据库密码: vuecmf                              │
│                                                  │
│  存储的应用用户:                                 │
│  - username: admin                              │
│  - password_hash: $2a$10$...                    │
└─────────────────────────────────────────────────┘
```

---

## ✅ 已完成的修复

1. ✅ 创建数据库用户 `vuecmf` (密码: `vuecmf`)
2. ✅ 更新 `local.env` 配置使用 `vuecmf` 连接数据库
3. ✅ 更新 `admin` 用户的密码为 `admin123`
4. ✅ 更新服务凭证（central-brain）的密钥

---

## 🎯 快速参考

### 前端登录
- URL: `http://localhost:8081`
- 用户名: `admin`
- 密码: `admin123`

### 数据库连接
```bash
psql -h localhost -U vuecmf -d zervigo_mvp
# 密码: vuecmf
```

### 服务凭证
- Service ID: `central-brain`
- Service Secret: `central-brain-secret-2025`

---

## ⚠️ 安全提醒

这些是**开发环境**的默认配置！

生产环境部署前请务必：
1. 更改数据库密码
2. 更改管理员密码  
3. 更换服务密钥
4. 使用密钥管理系统

---

## 📞 如有疑问

如果遇到任何问题，请参考：
- 详细配置: `docs/DATABASE_CONFIGURATION.md`
- 启动流程: `imartdevos/完整启动流程.md`
- 故障排除: `imartdevos/troubleshooting.md`

